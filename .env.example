# Groq (server-side only)
GROQ_API_KEY=

# Upstash Redis (Vercel marketplace integration or direct Upstash)
# Either of these naming conventions works — code checks both.
KV_REST_API_URL=
KV_REST_API_TOKEN=
# or
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
